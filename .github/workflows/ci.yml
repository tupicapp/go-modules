name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    types: [ opened, edited, synchronize, reopened ]

jobs:
  test:
    uses: ./.github/workflows/test.yml
    with:
      environment: ci
    secrets: inherit
